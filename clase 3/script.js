//let repeticion = parseInt(prompt("Cuantas veces desea que se repita?"));

//for (let i = 1; i <= repeticion; i++) {
/*   //i=i+2 suma de a dos
  if (i % 2 == 0) {
    //modulo %  :resto de la division
    console.log(i);
  }
 */
//if (i == 5) {
//  break; //continue; saltea el 5
//}
//console.log(i);
//}
// != :distinto

// validacion de contrasenia

/* let pass = 1234;
let dato = parseInt(prompt("Ingrese la contrasenia"));
let intentos = 3; //queremos que el usuario tenga 3 intentos.

while (dato != pass && intentos > 0) {
  //operadores logicos && para combinar dos condiciones
  intentos--; //para restar un intento -- resta 1; intentos=intentos-2 resta 2;
  alert(`la contrasenia es erronea. te qurdan ${intentos} intentos.`);
  dato = parseInt(prompt("Ingrese la contrasenia"));
}
 */

/* hacer una calculadora 
1- le vamos a pedir al usuario un numero
2- le pedimos el segundo numero
3- le pedimos la operacion quiere realizar

switch - va a evaluar la operacion y a calcularla - 

*/

/* let num1 = parseInt(prompt("cual es el primer numero"));
let num2 = parseInt(prompt("cual es el segundo numero"));
let operacion = prompt("cual es la operacion? +, -, *, /");
let resultado = 0;

switch (operacion) {
  case "+":
    resultado = num1 + num2;
    console.log(resultado);
    break;
  case "-":
    resultado = num1 - num2;
    console.log(resultado);
    break;
  case "*":
    resultado = num1 * num2;
    console.log(resultado);
    break;
  case "/":
    resultado = num1 / num2;
    console.log(resultado);
    break;
  default:
    alert("esto no es una operacion matematica");
    break;
} */
