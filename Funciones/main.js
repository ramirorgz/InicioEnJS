/* const numero = (num1, num2) => {
  return `el numero es : ${num1 + num2}`;
};
const resultado = numero(10, 30);
console.log(resultado); */

const numero = (num1, num2) => `el numero es : ${num1 + num2}`;

const resultado = numero(10, 30);
console.log(resultado);

/* ES LO MISMO */
